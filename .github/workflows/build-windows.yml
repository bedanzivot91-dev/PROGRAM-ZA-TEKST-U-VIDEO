name: Build Windows installer

on:
  push:
    branches:
      - main
      - "chatgpt/**"
    paths:
      - "SOURCE/**"
      - "INSTALIRAJ-PROGRAM.bat"
      - "SASTAVI-PROGRAM.bat"
      - ".github/workflows/build-windows.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: SOURCE
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: SOURCE/package-lock.json

      - name: Install FFmpeg
        shell: pwsh
        working-directory: ${{ github.workspace }}
        run: |
          choco install ffmpeg -y --no-progress
          ffmpeg -version
          ffprobe -version

      - name: Install dependencies
        run: npm ci

      - name: Windows JavaScript syntax check
        shell: pwsh
        run: |
          node --check "PROGRAM - NE BRISATI/public/app.js"
          node --check "PROGRAM - NE BRISATI/public/completion-ui.js"
          if ($LASTEXITCODE -ne 0) {
            Write-Host "Node syntax check failed"
            exit $LASTEXITCODE
          }

      - name: Static tests
        run: npm run test:static

      - name: Server tests
        run: npm run test:server

      - name: Electron desktop smoke tests
        run: npm run test:electron

      - name: Complete module tests
        run: npm test

      - name: Build NSIS and portable EXE
        run: npm run dist:all

      - name: Silent NSIS install and uninstall smoke test
        shell: pwsh
        run: |
          $setup = Get-ChildItem -Path dist -Filter "Muzicki-Spot-Studio-Free-Setup-v*.exe" | Select-Object -First 1
          if (-not $setup) { throw "NSIS setup EXE nije pronađen." }
          $installDir = Join-Path $env:RUNNER_TEMP "MSS-Installed"
          if (Test-Path $installDir) { Remove-Item $installDir -Recurse -Force }
          $proc = Start-Process -FilePath $setup.FullName -ArgumentList "/S", "/D=$installDir" -Wait -PassThru
          if ($proc.ExitCode -ne 0) { throw "NSIS silent install nije uspeo. ExitCode=$($proc.ExitCode)" }
          if (-not (Test-Path $installDir)) { throw "Install folder nije napravljen." }
          $installedExe = Get-ChildItem -Path $installDir -Filter "*.exe" -File | Where-Object { $_.Name -notmatch "(?i)unins|uninstall" } | Select-Object -First 1
          if (-not $installedExe) { throw "Instalirana aplikacija EXE nije pronađena." }
          $uninstaller = Get-ChildItem -Path $installDir -Filter "*.exe" -File | Where-Object { $_.Name -match "(?i)unins|uninstall" } | Select-Object -First 1
          if (-not $uninstaller) { throw "Uninstaller nije pronađen." }
          $unproc = Start-Process -FilePath $uninstaller.FullName -ArgumentList "/S" -Wait -PassThru
          if ($unproc.ExitCode -ne 0) { throw "NSIS silent uninstall nije uspeo. ExitCode=$($unproc.ExitCode)" }
          Start-Sleep -Seconds 2
          if (Test-Path $installedExe.FullName) { throw "Aplikacioni EXE je ostao posle uninstall-a." }

      - name: Upload Windows installers
        uses: actions/upload-artifact@v4
        with:
          name: Muzicki-Spot-Studio-Free-Windows-v15.6.0
          path: |
            SOURCE/dist/*.exe
            SOURCE/dist/*.yml
            SOURCE/dist/*.blockmap
          if-no-files-found: error
          retention-days: 30
